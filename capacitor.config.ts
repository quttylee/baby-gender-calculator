import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.babygender.calculator',
  appName: '재미로 보는 아기성별계산기',
  webDir: 'dist/public'
};

export default config;
